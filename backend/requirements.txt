fastapi==0.115.5
uvicorn[standard]>=0.41.0
python-dotenv==1.0.1
pydantic>=2.11.0,<3.0.0
amadeus==9.0.0
google-genai==0.6.0
google-generativeai>=0.8.6
googlemaps==4.10.0
httpx==0.27.2
aiofiles==24.1.0
tenacity==9.0.0
motor==3.6.0
pymongo>=4.9,<4.10
dnspython>=2.0.0
psutil==5.9.8
redis==7.1.0
omise==0.10.0
passlib[bcrypt]==1.7.4
watchdog==6.0.0  # For dev_server.py auto-reload
firebase-admin>=6.8.0  # Firebase Admin SDK for authentication
python-dateutil>=2.8.2  # Date parsing for email verification
# Data processing (for Data Normalization)
numpy>=2.4.2  # Numerical computing (used in data_normalization.py)
# Machine Learning (keyword/intent decoding & validation)
scikit-learn>=1.8.0  # ML (LR) + MLP+Calibrated for workflow intent classification
# LangChain - Orchestration layer (LCEL, chains, tool binding)
langchain>=0.3.28
langchain-core>=0.3.83
langchain-google-genai>=2.0.10
langgraph>=1.0.1  # Workflow state machine (planning → searching → selecting → summary → booking → done)
langgraph-checkpoint-redis>=0.3.6  # Redis checkpointer for LangGraph (resume Agent Mode per session)