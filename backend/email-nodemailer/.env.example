# คopiไปที่ backend/.env (บริการนี้อ่านจาก backend/.env)
# หรือสร้าง .env ในโฟลเดอร์นี้ก็ได้

# พอร์ตของบริการส่งอีเมล (default 3025)
# EMAIL_SERVICE_PORT=3025

# SMTP (ใช้ Gmail / Outlook / เซิร์ฟเวอร์อื่น)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# FROM_EMAIL="AI Travel Agent" <your-email@gmail.com>

# ใน backend/.env ตั้งค่าให้ Python เรียกบริการนี้:
# EMAIL_SERVICE_URL=http://localhost:3025
